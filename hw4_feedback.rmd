---
title: "Template for hw4"
output: html_notebook
---

Це ноутбук з фідбеком. До кожного завдання будуть коменти та/або код, якщо десь є помилки або щось не вдалось

```{r}
library(tidyverse)
library(lubridate)
library(rvest)
```

## Підготовка

```{r}
air <- read_html("https://www.radarbox.com/statistics/total") %>%
  html_node("table#datatable") %>%
  html_table() %>%
rename(date = Day)

colnames(air) <- colnames(air) %>%
  str_replace("  ", "_") %>%
  str_replace(" ", ".") %>%
  str_to_lower()
```

## Завдання
 
```{r}
air <- air %>%
  mutate(date = as.Date(date)) # %>%
  # group_by(week(date)) — закоментувала бо групбай зайвий тут. 1 дата = 1 рядок, тому таке групування не має сенсу
  air
  
```

### 2
Все вірно. Лапки можна прирати, але і з ними все працює.
```{r}

air <- air %>%
  mutate(date = as.Date(date)) %>%
  select(date, china_non.china)  
air
  
```

### 3. Сортування
Є, посортували
```{r}
# df <- read.csv("checkpoints/df-2.csv")

air <- air %>%
  arrange(date)  # є зайві дужки, на роботу коду не впливають але нащо тримати ось зайве?
 
air
```

### 4. Розрахунок нових змінних
Тут все супер-правильно
```{r}
# df <- read.csv("checkpoints/df-3.csv")
air <- air %>%
  mutate(year = year(date),
          month = month(date),
          day = day(date))
 air

```


### 5. Фільтрування з групуванням
Все правильно, знаю, це було складно)
```{r}
# df <- read.csv("checkpoints/df-4.csv")
air <- air %>%
  group_by(day,month) %>%
  filter(n() == 2) %>%
  ungroup()  # додамо відступ для краси
air
  
```


### 6. Перетворення

```{r}
# df <- read.csv("checkpoints/df-5.csv")
wider_df <- air %>% 
pivot_wider(id_cols = c("day", "month"),
              names_from = "year",
              values_from = "china_non.china",
              names_prefix = "y")
wider_df
```

Тут теж все ок
```{r}
# wider_df <- read.csv("checkpoints/df-5-wider.csv")

wider_df <- wider_df %>% 
mutate(flights = y2020/y2019,
       date = str_c("2020", month, day, sep="-"),
       date = ymd(date))

 

wider_df



  # ваш код
```



### 7. З'єднання

```{r}
prepare_covid <- function(url, col_name) {
  d <- read.csv(url) %>%
    rename(region = Province.State, country = Country.Region) %>%
    pivot_longer(cols = -c(region, country, Lat, Long),
                 names_to = "date",
                 names_prefix = "X",
                 values_to = col_name
                 ) %>%
    mutate(date = mdy(date)) %>%
    select(-c(Lat, Long)) %>%
    group_by(country, date) %>%
    summarise(col_name = sum(get(col_name)))
  
  cnames <- colnames(d)
  cnames[cnames == "col_name"] <- col_name
  colnames(d) <- cnames
  return(d)
}
covid19 <- prepare_covid("https://bit.ly/3aLLfKw", "cases") %>%
  full_join(prepare_covid("https://bit.ly/2XdZ6W0", "recovered")) %>%
  full_join(prepare_covid("https://bit.ly/2yEhPQg", "deaths")) %>%
  mutate(active_cases = cases - recovered - deaths,
         date = as.Date(date))
covid19
```


```{r}
vector_df <- covid19 %>% 
 filter(country=="China") 
vector_df 
```

Теж все вірно. Щоб уникнути NA у даних за січень можна зробити left_join
```{r}
df <- vector_df %>%
 filter(country=="China") %>%
 # full_join(wider_df,by=c("date"))
  left_join(wider_df,by=c("date"))
 
df



```

```{r}
# wider_df <- read.csv("checkpoints/df-6-wider.csv") %>%
#   mutate(date = as.Date(date))


```

### 8. Збережіть отримані з'єднані дані в csv.

```{r}
write.csv(df,"POLINA.csv")


```


### Що вийшло в результаті 
```{r}

read.csv("POLINA.csv")
```


